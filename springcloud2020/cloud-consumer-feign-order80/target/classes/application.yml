server:
  port: 80

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7003.com:7003/eureka/

logging:
  level:
    # feign日志以什么级别监控哪个接口
    com.andy.springcloud.service.PaymentFeignService: debug

